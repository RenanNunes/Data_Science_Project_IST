import pandas as pd


def preprocessing(dataframe: pd.DataFrame):
    report = ''
    report += '1.Data exploration\n'
    report += '1.1 Data Shape: ' + dataframe.shape
    return report


def unsupervised(dataframe: pd.DataFrame):
    return


def classification(dataframe: pd.DataFrame):
    return
